#flux
  .row
    .small-12.columns
      %h2= t 'flux.new.h2', site: @new.site.nom
  .row
    .small-12.columns
      .text-center.button-group.expanded
        = button_group_link(@new.site)
        = button_group_rss(@new.site)
        = button_group_news(@new.site)
      = render partial: 'new', locals: { uneNews: @new, leSite: @new.site }
