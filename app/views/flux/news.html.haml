#flux
  .row
    .small-12.columns
      %h2= t 'flux.new.h2', site: @site.nom
  .row
    .small-12.columns
      = render partial: 'site', locals: { leSite: @site}
      = render partial: 'new', locals: {uneNews: @news, leSite: @site}
